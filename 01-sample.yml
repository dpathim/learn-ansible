- name: Install Nginx
  hosts: Web
  become: true
  tasks:
    - name: Install Nginx
      ansible.builtin.yum:
        name: httpd
        state: latest

    - name: Start Nginx
      ansible.builtin.systemd:
        state: started
        name: nginx
        enabled: true



